/**
 * Area chart wrapper for fund performance visualization.
 * Uses recharts under the hood with Maybern DS tokens.
 *
 * TODO: Implement with actual recharts AreaChart component
 */
export default function AreaChartPlaceholder() {
  return (
    <div className="flex items-center justify-center rounded-lg border border-[var(--border-subtle)] bg-[var(--bg-layer-02)] p-[var(--spacing-09)]">
      <span className="text-text-tertiary type-caption-01">
        Area Chart — connect to recharts
      </span>
    </div>
  );
}
